bitcore-node:
  build: .
  dockerfile: Dockerfile
  container_name: blockbook-testnet
  ports:
    - "9030:9030"
    - "9130:9130"
  volumes:
    - ../data:/opt/coins/data/bcash/blockbook/db
    - ../logs:/opt/coins/blockbook/bcash/logs
  restart: always
